ANTHROPIC_API_KEY=

# Optional. The separate private skills repository, checked out next to this one, as a path
# relative to this folder (for example ../my-private-skills). Unset: public skills only.
# Set: `pnpm skills:validate` also checks the skills in <that repository>/skills/.
PRIVATE_SKILLS_ROOT=
